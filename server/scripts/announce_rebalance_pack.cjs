const { Pool } = require('pg');
const pool = new Pool({
  connectionString: 'postgresql://postgres:kkdZoXIuKmAadyDcyOVzhPlJpiitIBhG@maglev.proxy.rlwy.net:53059/railway',
  max: 1,
});

const TITLE = '[패치] 마법사/도적 리밸런스 · 허수아비 존 · 잠금 아이템 장착 외';
const BODY = `━━━━━━━━━━━━━━━━━━━━━━━━
마법사 클래스 리밸런스
━━━━━━━━━━━━━━━━━━━━━━━━
· 신규 패시브: 지능 1당 마법 데미지 +0.25%
  상위 법사 기준 대략 +40~50% 마법 데미지 상승
· 도트 즉발화: 화염/도트 계열 스킬 시전 시 총 도트 피해의 50%를 즉시 추가 적용
  (1타킬 구간에서 도트가 낭비되지 않도록 실사냥 보정)
· 원소 공명: 몬스터 처치 시 초과 피해의 50%가 다음 스폰되는 몬스터에게 자동 전이
  (잉여 피해를 다음 사냥감으로 이월, 반드시 1 이상의 체력은 남김)

━━━━━━━━━━━━━━━━━━━━━━━━
도적 클래스 리밸런스
━━━━━━━━━━━━━━━━━━━━━━━━
· 신규 패시브: 독의 공명
  도적이 적에게 독 스택을 쌓을 때마다 게이지 +1, 최대 10까지 충전
  10 도달 시 다음 행동 시작 때 현재 적이 가진 모든 독 피해의 2배를 즉시 폭발
  독 스택 자체는 유지되므로 폭발 후에도 도트 피해가 계속 들어감
· 게이지는 전투 화면 상단에서 확인 가능

━━━━━━━━━━━━━━━━━━━━━━━━
허수아비 존 (딜 체크용 사냥터)
━━━━━━━━━━━━━━━━━━━━━━━━
· 사냥터 목록 맨 아래에 "허수아비 존 Lv.10" 추가
· 허수아비는 절대 죽지 않고 공격하지 않으며, 경험치/골드/드랍 전부 없음
· 전투 화면 상단에 누적 데미지 / 경과 시간 / 초당 딜량이 실시간 표시
· 측정 초기화 버튼으로 언제든 리셋 가능
· 다양한 세팅/빌드/접두사/노드 조합의 딜을 비교하는 용도로 사용해 주세요

━━━━━━━━━━━━━━━━━━━━━━━━
잠금 아이템 장착/해제 허용
━━━━━━━━━━━━━━━━━━━━━━━━
· 잠금이 걸린 아이템도 장착과 해제가 가능하도록 변경
· 판매/분해/우편/거래소 송부는 이전과 동일하게 잠금이 유지돼 차단됨
· 장착/해제 과정에서 잠금 상태는 그대로 보존

━━━━━━━━━━━━━━━━━━━━━━━━
초월 노드(8포인트) 학습 조건 완화
━━━━━━━━━━━━━━━━━━━━━━━━
· 초월 노드 학습 시 요구되던 "노드 스크롤 +8" 소비가 제거됨
· 노드 포인트만 충분하면 누구나 바로 학습 가능
· 직업당 1개 학습 제한은 그대로 유지

━━━━━━━━━━━━━━━━━━━━━━━━
버그 수정
━━━━━━━━━━━━━━━━━━━━━━━━
· "마력 집중"과 "마력 과부하"를 동시에 사용하면 과부하의 자기 속도 감소 효과가 상쇄되어 +80% 마법 피해 조건이 발동되지 않던 문제 수정
· "무쌍난무"가 치명타로 적중했을 때 "재충전" 계열 접두사 효과가 발동되지 않던 문제 수정

━━━━━━━━━━━━━━━━━━━━━━━━
기타
━━━━━━━━━━━━━━━━━━━━━━━━
· 마법사 전체 스킬 설명에 "지능 1당 마법 데미지 +0.25%" 표기 추가
· 사냥터 목록에서 허수아비 존은 레벨 순으로 맨 아래에 정렬

피드백 주신 유저분들께 감사드리며, 추가 조정이 필요한 부분은 지속적으로 살펴보겠습니다.`;

(async () => {
  const adminRow = await pool.query("SELECT id FROM users WHERE username = 'admin' LIMIT 1");
  const authorId = adminRow.rows[0]?.id ?? null;
  await pool.query(
    `INSERT INTO announcements (title, body, priority, expires_at, author_id, active)
     VALUES ($1, $2, 'urgent', NOW() + INTERVAL '14 days', $3, TRUE)`,
    [TITLE, BODY, authorId]
  );
  console.log('공지 등록 완료 (urgent, 14일)');
  await pool.end();
})().catch(e => { console.error(e); process.exit(1); });
